git init = initiate a project

git checkout branch_name: go to branch_name

git checkout -b branch_name: create branch_name and go to that branch

git branch branch_name


# to commit
git add file_name

# for all
git add .

# if you want to merge two branches
# go to the branch you want to merge to
# once in it type: 
git merge name_of_branch_you_want_to_merge_to_this_branch